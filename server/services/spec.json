{
  "openapi": "3.0.0",
  "servers": {
    "url": "/"
  },
  "paths": {
    "/api/opensearch/mca": {
      "post": {
        "tags": [
          "all chromosome"
        ],
        "summary": "Finds events",
        "produces": ["application/json"],
       "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/definitions/summaryRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful operation"
          },
          "400": {
            "description": "Invalid parameters supplied"
          }
        }
      }
    }  
  },
   "definitions":{
    "summaryRequest":{
      "type":"object",
      "properties":{
        "types":{
          "type": "string",
          "description": "Specifies the study dataset",
          "enum": ["plco", "ukbb"],
          "default": ["plco"]

        }
      }
    }
   }
}